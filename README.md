# image-captioning-system
An end-to-end Image Captioning system that generates natural language descriptions from images using deep learning. The project implements both LSTM-based and Transformer-based encoder-decoder architectures, enabling comparison between recurrent and attention-based sequence modeling approaches.
